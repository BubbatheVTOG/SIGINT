# Desktop Host Configuration (matt)

Store host-specific configurations for `matt` here (e.g., `.config/hypr/hyprland.lua`, monitor layouts, waybar configs).

To stow on `matt`:
```bash
stow -R -t ~ common matt
```
